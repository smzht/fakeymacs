#+STARTUP: showall indent

* Fakeymacs extension - vim_key -

** ■ Vim 用のキーの設定を行う

Vim 用のキーの設定を行う拡張機能です。Vim のキーバインドを活かしながら、Emacs のキーバインドが
追加で利用可能となります。

Windows Terminal 上の Linux で Vim を動かす場合を想定して作成しています。

*** 環境設定

本拡張機能を使うために、次の設定を行ってください。

**** 1) 本拡張機能を有効にする

Keyhac のトップフォルダにある config_personal.py ファイルにおいて、vim_key 拡張機能の設定箇所
を有効（if 0: を if 1: に変更）にしてください。

**** 2) 必要に応じて config_personal.py ファイルを作成する

必要に応じて、本フォルダにある _config_personal.py を config_personal.py というファイル名
に複写して作成してください。
config_personal.py を作成することで、本拡張機能の個人設定ファイルとして機能します。

**** 3) .vimrc の設定を行う

~/.vimrc に次の設定を行ってください。

#+BEGIN_EXAMPLE
set nocompatible
set title
set number
set selection=exclusive
set virtualedit=onemore
set whichwrap=b,s,h,l,<,>,[,]
#+END_EXAMPLE

*** コンフィグレーションパラメータ

|-------------------------+----------------------------------------------------------------|
| Configuration parameter | Description                                                    |
|-------------------------+----------------------------------------------------------------|
| fc.vim_target           | Vim 用のキーバインドを利用するアプリケーションソフトを指定する |
|-------------------------+----------------------------------------------------------------|

*** キーバインド

**** ● 「インサートノーマルモード移行」のキー設定

|---------+--------------------------+-------------|
| Keybind | Function                 | Description |
|---------+--------------------------+-------------|
| C-A-o   | enter_insert_normal_mode |             |
|---------+--------------------------+-------------|

**** ● 「ファイル操作」のキー設定

|-----------+-------------+-------------|
| Keybind   | Function    | Description |
|-----------+-------------+-------------|
| Ctl-x C-f | find_file   |             |
| Ctl-x C-s | save_buffer |             |
| Ctl-x C-w | write_file  |             |
|-----------+-------------+-------------|

**** ● 「カーソル移動」のキー設定

|---------------------------+------------------------+-------------|
| Keybind                   | Function               | Description |
|---------------------------+------------------------+-------------|
| C-b                       | backward_char          |             |
| C-f                       | forward_char           |             |
| M-b                       | backward_word          |             |
| M-f                       | forward_word           |             |
| C-p                       | previous_line          |             |
| C-n                       | next_line              |             |
| C-a                       | move_beginning_of_line |             |
| C-e                       | move_end_of_line       |             |
| M-<                       | beginning_of_buffer    |             |
| M->                       | end_of_buffer          |             |
| M-g g                     | goto_line              |             |
| M-g M-g                   | goto_line              |             |
| C-l                       | recenter               |             |
| fc.scroll_key[0] の設定値 | scroll_up              | 初期値：M-v |
| fc.scroll_key[1] の設定値 | scroll_down            | 初期値：C-v |
|---------------------------+------------------------+-------------|

**** ● 「カット / コピー / 削除 / アンドゥ」のキー設定

|-----------+----------------------+--------------------------------------------|
| Keybind   | Function             | Description                                |
|-----------+----------------------+--------------------------------------------|
| C-h       | delete_backward_char |                                            |
| C-d       | delete_char          |                                            |
| M-Delete  | backward_kill_word   |                                            |
| M-d       | kill_word            |                                            |
| C-k       | kill_line            |                                            |
| C-w       | kill_region          |                                            |
| M-w       | kill_ring_save       |                                            |
| C-y       | yank                 |                                            |
| C-/       | undo                 |                                            |
| Ctl-x u   | undo                 |                                            |
| C-_       | undo                 |                                            |
| C-@       | set_mark_command     |                                            |
| C-Space   | set_mark_command     |                                            |
| Ctl-x h   | mark_whole_buffer    |                                            |
| Ctl-x C-p | mark_page            |                                            |
|-----------+----------------------+--------------------------------------------|

※ undo の機能は、C-g の押下により redo に切り替わります（以降、トグル動作）。

**** ● 「テキストの入れ替え」のキー設定

|---------+-----------------+-------------|
| Keybind | Function        | Description |
|---------+-----------------+-------------|
| C-t     | transpose_chars |             |
|---------+-----------------+-------------|

**** ● 「文字列検索」のキー設定

|---------+------------------+-------------|
| Keybind | Function         | Description |
|---------+------------------+-------------|
| C-r     | isearch_backward |             |
| C-s     | isearch_forward  |             |
|---------+------------------+-------------|

**** ● 「キーボードマクロ」のキー設定

|---------+---------------------------+-------------|
| Keybind | Function                  | Description |
|---------+---------------------------+-------------|
| Ctl-x ( | kmacro_start_macro        |             |
| Ctl-x ) | kmacro_end_macro          |             |
| Ctl-x e | kmacro_end_and_call_macro |             |
|---------+---------------------------+-------------|

**** ● 「矩形選択」のキー設定

|-----------+---------------------------+-------------|
| Keybind   | Function                  | Description |
|-----------+---------------------------+-------------|
| C-A-Space | rectangle_mark_mode       |             |
|-----------+---------------------------+-------------|

**** ● 「その他」のキー設定

|-----------+------------------------+-------------------------------------|
| Keybind   | Function               | Description                         |
|-----------+------------------------+-------------------------------------|
| C-m       | newline                |                                     |
| C-i       | indent_for_tab_command | fc.use_ctrl_i_as_tab が True の場合 |
| C-g       | keyboard_quit          |                                     |
| Ctl-x C-c | kill_emacs             |                                     |
|-----------+------------------------+-------------------------------------|

*** 留意事項

● 本拡張機能は、Vim のエキスパートの利用は想定していません。Emacsキーバインドユーザが環境条件
により Vim を使う必要が出た場合に、Vim の基本機能を Emacs キーバインドで操作できるようにするために
作成したものです。Vim の拡張機能に対しての動作は確認しておりませんので、ご了承ください。

● 本拡張機能では、IMEの状態を自動で変更する場合があります。このため、テキスト カーソル インジケータ
を有効とし、IME の状態を色で表示することをおすすめします。テキスト カーソル インジケータを有効と
する方法は、次を参照してください。

-  https://faq.nec-lavie.jp/qasearch/1007/app/servlet/relatedqa?QID=022081

また、Fakeymacs のトップディレクトにある config_personal.py 内の fc.use_ime_status_cursor_color
変数を True にする必要があります。

● Windows Terminal でテキスト カーソル インジケータを使っていると、表示位置が 2行くらいずれる場合が
あります。この場合は、新しいターミナルをタブで一度開くと是正されるようです。

● ノーマルモードで日本語を入力すると、インサートモードに移行した後、日本語が入力される仕様としています。

● ssh の接続先で Vim を使う場合、接続先で上記で紹介した .vimrc の設定を行えば、接続元で本拡張機能
が有効となり、Emacs キーバインドが利用できるようになります。（.vimrc の中にある set title が、
ターミナルのタイトルを変更することにより、実現がされています。）

● ノーマルモードでカーソルを１カラム目に置いて行を選択してのコピペを行うと、ペースト後にカーソルの
位置が１カラム右にずれます。これを回避するには、インサートモードでコピペを行うか、ペーストのコマンドに
P（S-p）を使うようにしてください。

● Emacs キーバインドで実行されるコマンドは、Vim のノーマルモードでもインサートモードでも動作するよう
に作成していますが、文字の入力をする場合は、Vim のキー（i や a など）でインサートモードに明示的に
移行する必要があります。

● 本拡張機能では、Vimのいくつかのモードの状態を変数で管理しています。このモードの状態と変数の設定が
ずれると正常に動作しませんので、その場合は C-g を押下して状態の初期化を行ってください。
